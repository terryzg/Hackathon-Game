importTotal_health = 5
health = int(input("input health 1-100")) # that is not in my game but for test purposes
brb = "x"
other_brb = "--"

#bar has to be 10 long so it is divinding and rounding 
total_hp = Total_health % 5

#multiples x to represent hp
hp = health % 5
end_bar = total_hp - hp
bar = (hp * brb) 
m = (end_bar * other_brb)
#adds bars together
print(bar + m)